services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: church
      POSTGRES_USER: church
      POSTGRES_PASSWORD: church
    ports:
      - "5432:5432"
    volumes:
      - church_pg_data:/var/lib/postgresql/data

volumes:
  church_pg_data:
